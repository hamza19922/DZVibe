# DZVibe startup stabilization

The Android release is temporarily using a safe startup shell: authentication and navigation initialize before any native video player is mounted. The video feed will be restored after the startup APK is verified on-device.
